# "xdotool getwindowfocus key --window %1 click 2"
#  Mod1 + v

# "sh -c 'xclip -r -o -sel clipboard | xvkbd -delay 0 -file - 2>/dev/null'"
#  Mod1 + v

#"xvkbd -no-sync -no-jump-pointer -text "\D1`xclip -r -o -sel clipboard`" 2>/dev/null"
#  Mod1 + v

# copy text
"xset r off; xdotool keyup Alt keyup c; xclip -o -sel primary | xclip -sel clipboard; xset r on"
  Alt + c

# paste text
"xset r off; xdotool keyup Alt keyup v type --delay 0 -- "$(xclip -o -sel clipboard)"; xset r on"
  Alt + v
